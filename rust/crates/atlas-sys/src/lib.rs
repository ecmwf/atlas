//! Build-only crate for ECMWF atlas (grid/mesh library).
//!
//! Provides build paths for downstream crates that depend on atlas.
//! No Rust API — just builds the C++ library and exports `DEP_ATLAS_ROOT`.
